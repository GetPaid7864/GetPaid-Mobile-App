import 'package:flutter/material.dart';

import '../body/background_check_body.dart';

class BackgroundCheckView extends StatelessWidget {
  const BackgroundCheckView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const BackgroundCheckBody();
  }
}
