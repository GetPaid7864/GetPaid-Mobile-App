import 'package:flutter/material.dart';

class JobSeekerContractsScreen extends StatelessWidget {
  const JobSeekerContractsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
