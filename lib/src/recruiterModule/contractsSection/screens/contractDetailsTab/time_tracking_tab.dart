import 'package:flutter/material.dart';

class TimeTrackingTab extends StatelessWidget {
  const TimeTrackingTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [


        ],
      ),
    );
  }
}
