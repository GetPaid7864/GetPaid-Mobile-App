import 'package:flutter/material.dart';

class ActiveContracts extends StatelessWidget {
  const ActiveContracts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
