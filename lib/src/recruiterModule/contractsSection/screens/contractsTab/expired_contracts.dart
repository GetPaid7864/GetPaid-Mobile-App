import 'package:flutter/material.dart';

class ExpiredContracts extends StatelessWidget {
  const ExpiredContracts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
